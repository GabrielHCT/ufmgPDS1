#include <stdio.h>

int main() {
    int valor, par = 0, impar = 0;

    scanf("%d", &valor);

    valor % 2 == 0 && valor > 0 ? par = 1 : par = 0;
    valor % 2 == 1 && valor > 0 ? impar = 1 : impar = 0;
    
    printf("")

    return 0;
}